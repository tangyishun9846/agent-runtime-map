{
  "name": "agent-runtime-map-workspace",
  "version": "0.2.0",
  "private": true,
  "description": "Turn your codebase into an evidence-backed Agent Runtime Map.",
  "homepage": "https://github.com/tangyishun9846/agent-runtime-map#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tangyishun9846/agent-runtime-map.git"
  },
  "bugs": {
    "url": "https://github.com/tangyishun9846/agent-runtime-map/issues"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "adapters/*"
  ],
  "scripts": {
    "build": "npm run build -w @agent-runtime-map/schema && npm run build -w @agent-runtime-map/analysis-kit && npm run build -w @agent-runtime-map/project-reader && npm run build -w @agent-runtime-map/react && npm run build -w @agent-runtime-map/typescript && npm run build -w @agent-runtime-map/python && npm run build -w @agent-runtime-map/logic-compiler && npm run build -w @agent-runtime-map/semantic && npm run build -w @agent-runtime-map/core && npm run build -w @agent-runtime-map/viewer && npm run build -w agent-runtime-map",
    "clean": "npm run clean --workspaces --if-present",
    "dev": "npm run build -w @agent-runtime-map/viewer && tsx packages/cli/src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "agent-runtime-map": "tsx packages/cli/src/cli.ts",
    "logic-map": "tsx packages/cli/src/cli.ts",
    "check": "npm run typecheck && npm test && npm run build",
    "release:check": "npm run check && npm audit --audit-level=high && node scripts/check-package.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.0",
    "tsup": "^8.5.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "esbuild": "^0.28.2"
  }
}
